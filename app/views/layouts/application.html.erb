<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> 
    <%# <%= javascript_importmap_tags %> 
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light shadow p-2 mb-3 bg-body rounded">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Blog App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="#">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Link</a>
            </li>
          </ul>
          <li class="nav-item d-flex">
            <% if user_signed_in?%>
              <%= link_to 'Edit profile', edit_user_registration_path, class: "btn btn-outline-info mx-2" %>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
              <img class="rounded-circle position-fixed end-0 mt-5 col-1 mx-4" src = <%= current_user.photo if user_signed_in? %> />
            <% else %>
              <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-secondary mx-2" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-info" %>
            <% end %>
          </li>
        </div>
      </div>
    </nav>

    <p class="notice text-success"><%= notice %></p>
    <p class="alert text-danger"><%= alert %></p>
    <%= yield %>
    <script src="https://kit.fontawesome.com/b331332bf9.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
